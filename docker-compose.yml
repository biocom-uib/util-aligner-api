version: '3'

services:
  api:
    image: bielr/util-aligner-api
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    command: sh docker/gunicorn.sh --timeout 720
    ports:
      - 10080:8080
    volumes:
      - .:/opt/:rw
      - data:/opt/data

volumes:
  data: {}
