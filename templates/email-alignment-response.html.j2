<!doctype html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html; charset=utf-8">

    <style type="text/css">
      body {
        font-family: sans-serif;
      }

      table {
        border-collapse: collapse;
        margin-left: 2em;
      }

      th, td {
        border: 1px solid #d0d0d0;
        padding: 0.8em;
      }

      td.htheader {
        background-color: #eeeeee;
        font-weight: bold;
      }
    </style>
  </head>

  <body>
    <p>
    {% if results['ok'] %}
      The alignment you requested has finished successfully with exit code {{ results['exit_code'] }}.
    {% elif scores is defined %}
      The alignment you requested has finished with errors (exit code {{ results['exit_code'] }}).
    {% elif 'timed_out' in results and results['timed_out'] %}
      The alignment you requested has timed out.
    {% else %}
      The alignment you requested has failed.
    {% endif %}
    </p>

    {% if results['exception'] %}
    <p>
      The following exception was caught during execution: {{ results['exception'] }}
    </p>
    {% endif %}

    <p>
    {% if results['ok'] %}
      You can see the result of this alignment <a href=https://datacom.uib.es/util-aligner-results/{{ result_id }} > here </a>
    {% else %}
      You can access the raw alignment result <a href=https://datacom.uib.es/util-aligner/v2/alignment/{{ result_id }}> here </a>
    {% endif %}
    </p>

    <p>
    If you submitted an alignment with more than one aligner, you will receive
    an email with comparison results after all of them have finished.
    </p>
  </body>
</html>
